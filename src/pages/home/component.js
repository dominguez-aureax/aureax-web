import React from 'react';
import { Alert } from 'react-bootstrap';

export default function Home() {
  console.log(process.env);
  return (
    <div className='home-wrapper'>
      <Alert variant='primary'>TODO: FINISH HOME PAGE</Alert>
    </div>
  );
}
